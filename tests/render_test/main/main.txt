float x = 100;
float y = 100;
float xspeed = 15;
float yspeed = 15;
int r=0, g=85, b=170;
void setup() {
  size(640,360);
  background(255);
}
 
void draw() {
  //background(255);
 
  x = x + xspeed;
  y = y + yspeed;
 
  if ((x > width) || (x < 0)) {
    xspeed = xspeed * -1;
  }
  if ((y > height) || (y < 0)) {
    yspeed = yspeed * -1;
  }
   
   
  
  fill(175);
  ellipse(x, y, 5, 5);
  
}